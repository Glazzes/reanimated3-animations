export type RGB = [number, number, number];
export type HSL = { h: number; s: number; l: number };

export type Vector = { x: number; y: number };
export type SizeVector = { width: number; height: number };
